PKG_CXXFLAGS = $(SHLIB_OPENMP_CXXFLAGS) -D_REENTRANT
PKG_LIBS = $(SHLIB_OPENMP_CXXFLAGS) $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS) $(shell "${R_HOME}/bin/Rscript.exe" -e "cat(RcppParallel::RcppParallelLibs())")
